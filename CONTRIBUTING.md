# Contributing

Use Node 22+, run `npm install` then `npm run check`. Never commit secrets, GGUF weights, generated bundles, or unverified binaries. Changes to permissions, downloads, plugins, MCP, or providers require abuse-case tests and documentation.
